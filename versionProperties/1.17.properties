# 1.17.x version (all minor versions)
java_version=16
minecraft_version=1.17.1
compatible_minecraft_versions=1.17.x
file_name=1.17
builds_for=fabric,forge,spigot

# Fabric loader
fabric_loader_version=0.18.1
fabric_api_version=0.46.1+1.17
    # Fabric mod versions
    #some_mod_version=1.0.0

    #{ "some_mod": "*" }
    fabric_incompatibility_list={}
    fabric_recommend_list={}

    # Fabric mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    #enable_some_mod=0

# Forge loader
forge_version=37.1.1
    # Forge mod versions
    #some_mod_version=1.0.0

    # Forge mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    #enable_some_mod=0

# Plugin loader
spigot_api_version=1.17.1-R0.1-SNAPSHOT
spigot_api_compatibility=1.17
