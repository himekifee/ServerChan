# 1.20.x version (all minor versions)
java_version=21
minecraft_version=1.20.6
compatible_minecraft_versions=1.20.x
file_name=1.20
builds_for=fabric,forge,neoforge,spigot
forgix_merge=fabric,forge,neoforge,spigot

# Fabric loader
fabric_loader_version=0.18.1
fabric_api_version=0.100.4+1.20.6
    # Fabric mod versions
    #some_mod_version=1.0.0

    #{ "some_mod": "*" }
    fabric_incompatibility_list={}
    fabric_recommend_list={}

# Fabric mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    #enable_some_mod=0

# (Neo)Forge loader
forge_version=50.1.9
neoforge_version=20.6.119
    # (Neo)Forge mod versions
    #some_mod_version=1.0.0

    # (Neo)Forge mod run
    # 0 = Don't enable and don't run
    # 1 = Can be referenced in code but doesn't run
    # 2 = Can be referenced in code and runs in client
    #enable_some_mod=0

# Plugin loader
spigot_api_version=1.20.6-R0.1-SNAPSHOT
spigot_api_compatibility=1.20
